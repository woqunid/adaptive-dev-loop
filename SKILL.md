---
name: adaptive-dev-loop
description: 当用户通过 /adaptive-dev-loop 显式唤醒、或丢入一个需要"从需求到可交付代码"完整落地的开发需求时使用本 skill。它驱动 AI 走完一条自适应研发闭环：需求接受 → 需求澄清 → 理解项目与影响分析 → 修改方案设计 → 任务拆分 → 编码实现 → 自测 → 代码审查；默认按天归档到 dev-loop/YYYY-MM-DD/需求短名/loop.md，复杂或审计场景才拆出 analysis.md，失败回流才拆出 iteration-log.md。落地前必须给出需求理解、验收标准、实现候选方案、推荐项和确认门记录。若用户明确授权"按推荐/最大选项/自动推进"，可自动选择推荐项继续，但必须记录授权依据。当自测或审查发现问题时，按问题根因自动回流到对应阶段循环修复，直到收敛或触达迭代上限。本 skill 会真实修改代码，属于重型、长耗时操作。务必使用本 skill 处理任何"实现这个需求 / 把这个功能做完 / 改完并自测自审"类的请求。
---

# 自适应研发闭环 (Adaptive Dev Loop)

把一个开发需求从"接收"一路推进到"自测、自审通过、可交付"的闭环流程。核心特征是**自适应循环**：自测或代码审查发现问题时，按问题根因自动回流到对应阶段重新修复，直到收敛或触达迭代上限。

本 skill 由用户**显式唤醒**（如 `/adaptive-dev-loop 给订单模块加批量导出`）。一旦唤醒即视为用户已授权走完整流程，无需再做规模分流判断——直接进入下面的步骤。本 skill **会真实修改项目代码**，属于重型、长耗时操作。

## 工作原则（先建立心智，再执行步骤）

这些原则贯穿全程，比单个步骤更重要：

- **每个阶段都有可检查的产物，并落成文件。** 默认把需求、分析、任务、自测、审查和交付结论写进 `dev-loop/YYYY-MM-DD/<需求短名>/loop.md`（见"产物目录"），让每一轮都能基于事实而非记忆推进，同时避免小需求产生一堆文件。
- **两个关键门用 grilling 盘问后再确认。** "需求澄清"和"修改方案设计"这两个门，先用盘问式访谈（`references/grilling.md`）和用户讨论清楚、达成共识，再输出"内容 + 验收标准"做二次确认；其余阶段（编码、自测、审查、循环修复）自动推进，不打断执行节奏。
- **确认门有两种执行模式，但都不能省略证据。** 默认交互模式下，需求门与方案门必须等待用户明确确认；若用户在本次请求中明确说"按你推荐""全程最大选项""自动推进"等预授权话术，则进入按推荐自动推进模式：仍要产出候选方案、推荐项、验收标准和授权依据，只是可以自动选择推荐项并继续。
- **修不好就升级给人，绝不无限重试。** 到达迭代上限或同一问题反复出现时，停下来向用户汇报根因和卡点，而不是继续盲目尝试。
- **遵循现有约定，不擅自扩大范围。** 模仿周边代码的风格与结构；只做需求范围内的事，发现"顺手能改的别的东西"先记录、不动手。
- **不靠掩盖来"修复"。** 不通过改测试断言、跳过用例、吞掉异常来让流程显得通过。详见 `references/boundaries.md`。

## 运行模式与确认门证据

先判断本次运行属于哪种模式，并写入 `loop.md` 的"确认门记录"；若已拆出 `analysis.md`，同步写入 `analysis.md` 的 ADR：

- **默认交互模式**：用户没有明确预授权时，步骤 2 和步骤 4 的确认门必须停下来等待用户回复。不能因为需求看起来简单就进入任务拆分或编码。
- **按推荐自动推进模式**：只有用户明确授权"按推荐 / 最大选项 / 自动推进 / 不用再问"等同义表达时才启用。启用后，仍必须在进入编码前写清：
  - 需求理解与逐条验收标准。
  - 2–3 个实现候选方案、各自利弊、推荐项和推荐理由。
  - 自动选择了哪个推荐项，以及授权依据来自哪句用户话。

不论哪种模式，`loop.md` 没有验收标准、实现候选方案与推荐项时，禁止进入步骤 5/6；若拆出 `analysis.md`，两边的选择结果必须一致。

## 阶段与产物总览

| # | 阶段 | 关键产物 | 门 |
|---|------|----------|-----|
| 1 | 需求接受 | 在 `dev-loop/YYYY-MM-DD/<需求短名>/` 下初始化 `loop.md` | — |
| 2 | 需求澄清 | `loop.md`：grilling 盘问 + 确认后的需求理解与验收标准 | **grilling+确认门** |
| 3 | 理解项目与影响分析 | `loop.md`：需求理解 / 当前项目现状（含原有规范）/ 涉及范围；复杂时拆 `analysis.md` | — |
| 4 | 修改方案设计 | `loop.md`：候选方案 / 推荐项 / 具体修改 / 风险 / 验证；复杂时同步 `analysis.md` | **grilling+确认门** |
| 5 | 任务拆分 | `loop.md`：可独立验证的小任务 + 验收标准 | — |
| 6 | 编码实现 | 代码改动 + 更新 `loop.md` 中任务状态 | — |
| 7 | 自测 | 构建/测试/lint 结果记入 `loop.md`；失败回流时拆 `iteration-log.md` | — |
| 8 | 代码审查 | 审查结论记入 `loop.md`（按 `references/review-checklist.md`） | — |
| ↺ | 自适应循环 | 发现问题 → 按根因回流到 4/5/6 或 2 → 重新走到 7、8 | — |
| ✓ | 交付汇报 | `loop.md` 的交付报告章节；完整审计模式可另存 `final-report.md` | — |
| 9 | 复盘与自我进化（仅"明显不顺"时触发） | 改进提议 → 审批 → 改 skill 自身 → 自检/回滚 → `_evolution/CHANGELOG.md` | **审批门** |

## 详细步骤

### 步骤 1 — 需求接受
取得需求。若用户调用时已带需求文本，以该文本为准；若未提供，先追问"请描述你要实现的需求（目标 + 范围）"。

为本次需求起一个简短的 kebab-case 名称，在项目根目录下创建产物目录 `dev-loop/YYYY-MM-DD/<需求短名>/`（日期为需求启动日，跨天继续使用原目录；若 `dev-loop/` 不存在则一并创建）；用 `references/loop-template.md` 初始化该目录的 `loop.md`，并把原始需求写入其中。本次运行的默认产物只放在这个文件里。

### 步骤 2 — 需求澄清 〔grilling + 确认门〕
用 **grilling 盘问式访谈**澄清需求（方法见 `references/grilling.md`）：一次只问一个聚焦问题、每个问题附上自己的推荐答案、能在代码里查到的先查不问，挑战有风险的假设，逐项理清目标、范围、边界情况、验收标准，直到达成共识。盘问过程中把结论与关键决策实时补进 `loop.md`。

**确认门**：盘问收敛后，把"最终需求理解 + 验收标准"完整输出给用户做二次确认。默认交互模式下，用户确认后才进入步骤 3；按推荐自动推进模式下，记录用户的预授权原话、自动采用推荐理解与验收标准，然后继续步骤 3。理解不一致会让后续整条路跑偏，这道门不能省，不能无记录跳过。

### 步骤 3 — 理解项目与影响分析
**编码前先理解项目，再分析影响——顺序不能颠倒。** 这是非破坏性的事实收集，默认写入 `loop.md`；只有涉及架构、公共接口、数据库、依赖、多模块影响、用户要求完整审计，或分析内容明显变长时，才按 `references/analysis-template.md` 拆出 `analysis.md`。按以下章节推进：

1. **需求理解**：把确认后的需求目标、范围、验收标准落到文档。
2. **当前项目现状**：实地阅读相关代码后再写——技术栈、目录与分层、**项目原有的规范与约定**（命名风格、代码组织、错误处理、日志、配置、测试组织与运行方式等），以及与本需求相关的现有实现是如何工作的。**这一节必须针对当前项目实际分析，不能写通用套话。**
3. **涉及范围**：会改到的文件/模块、受影响的调用方与上下游、相关测试、潜在回归点。

核心要求：本次改动要**融入并遵循项目原有规范，而不是另起一套新规范**。后续的方案与具体修改都以本节提炼出的现状为准绳。

### 步骤 4 — 修改方案设计 〔grilling + 确认门〕
在 `loop.md` 的方案章节中续写方案部分；若已拆出 `analysis.md`，同步在 `analysis.md` 中保留完整方案。**全程沿用步骤 3 提炼的原有规范**：

4. **修改方案**：在现有结构内如何实现的核心思路、关键改动点、接口/数据结构变化、取舍与放弃的备选方案。
5. **具体修改内容**：细化到文件/函数级——每个文件改什么、新增什么，并说明如何遵循原有规范。
6. **风险点**：技术风险、回归风险、对其他模块的影响、需用户拍板的高风险项。
7. **验证方式**：如何自测（构建/测试/lint）、每条验收标准如何验证、需新增或修改哪些测试。
8. **最终结论**：方案可行性与复杂度评估，以及需要用户在确认门拍板的事项。

**grilling + 两段式确认门**（方法见 `references/grilling.md`）：
1. **讨论选型**：就关键决策点逐个 grilling 盘问——给出 2–3 个候选方案及各自利弊和推荐项，一次聚焦一个分歧点，挑战风险假设，能查代码先查。把讨论与决策理由（ADR 式）记入 `loop.md`，让用户选定方案；复杂模式同步记入 `analysis.md`。
2. **正式确认**：方案选定后，把【完整修改方案 + 验收标准】输出给用户二次确认，尤其当涉及架构、公共接口、数据库 schema、依赖变更时。用户确认无误后，才进入下一轮（任务拆分与编码）。

默认交互模式下，上述两段都要等待用户明确选择/确认。按推荐自动推进模式下，可以自动选择推荐项，但必须在 `loop.md` 的 ADR 中记录候选方案、推荐项、选择结果、授权依据，并在进入步骤 5 前输出一次简明的"已按预授权采用推荐方案"摘要。

### 步骤 5 — 任务拆分
把方案拆成**可独立验证**的小任务，写入 `loop.md` 的任务章节，每个任务带一句话验收标准。任务粒度以"改完能单独自测"为宜；完整审计模式可同时拆出 `tasks.md`。

### 步骤 6 — 编码实现
按 `loop.md` 的任务清单逐个实现，遵循现有代码风格与约定，小步推进。每完成一个任务就在 `loop.md` 勾掉并简注改了什么；若拆出 `tasks.md`，同步更新。

### 步骤 7 — 自测
按 `references/review-checklist.md` 的"自测"部分执行：跑构建、测试、lint，验证每个任务的验收标准与需求的整体验收标准。把命令与结果（通过/失败、失败详情）记入 `loop.md`。若失败并进入回流，立即用 `references/iteration-log-template.md` 拆出 `iteration-log.md`，后续每轮回流同步记录。**"通过"必须有客观依据**（命令退出码、测试输出），不能凭感觉判定。

### 步骤 8 — 代码审查
以独立审查视角，按 `references/review-checklist.md` 的"审查"部分逐项检查：正确性、是否符合现有约定、是否引入回归、边界与错误处理、是否偏离需求范围。把审查结论记入 `loop.md`；若发现问题并回流，同步写入 `iteration-log.md`。

## 自适应循环机制（核心）

自测（步骤 7）或审查（步骤 8）发现问题后，**按根因分级回流**，而不是一律推倒重来：

| 问题根因 | 回流到 |
|----------|--------|
| 编码 bug、测试失败、实现写错 | 步骤 6 编码实现 |
| 漏改了某处、任务覆盖不全、有遗漏 | 步骤 5 任务拆分 |
| 方案本身有设计缺陷（架构/接口/数据流错误） | 步骤 4 修改方案设计 |
| 发现需求理解有误 | 步骤 2 需求澄清（重新触发确认门，需问用户） |

回流时**带上"这一轮为什么失败"的信息**重新进入该阶段，避免重复犯同样的错。首次回流时拆出 `iteration-log.md`，每轮都记录：发现的问题 → 根因定位 → 回到哪一步 → 做了什么修复 → 结果；`loop.md` 保留摘要并链接该文件。修复后重新走到步骤 7、8。

**终止与升级**（防死循环，必须遵守）：
- **收敛**：自测与审查均通过 → 进入交付汇报。
- **迭代上限**：默认最多 3 轮循环仍未收敛 → 停止，升级给用户。
- **同一问题反复**：同一问题连续出现 2 次 → 立即升级（说明当前路径无效，需要人介入）。
- **升级时**：把当前状态、已尝试的修复、根因判断、建议方向汇报给用户，由用户决策，而非继续盲目重试。

## 产物目录

本 skill 的所有产物（md）集中存放在**项目根目录下新建的 `dev-loop/` 文件夹**。每次新需求按启动日期和需求短名建目录，互不覆盖、便于按天回溯；跨天继续使用原目录，不按修改日拆分：

```
dev-loop/                         本 skill 所有产物的归档目录（项目根下）
└── YYYY-MM-DD/                    需求启动日期
    └── <需求短名>/                每个需求一个子目录
        ├── loop.md               默认唯一产物：需求、验收、方案、任务、自测、审查、交付
        ├── analysis.md           复杂/审计模式才拆出：项目现状、影响、方案、ADR
        ├── iteration-log.md      失败回流时才拆出：每轮问题、根因、修复、结果
        ├── tasks.md              完整审计模式可选：任务拆分与进度
        ├── final-report.md       完整审计模式可选：交付报告
        └── README.md             完整审计模式可选：目录索引
```

各文件作用（分类）：

| 类别 | 文件 | 作用 |
|------|------|------|
| 默认主文件 | `loop.md` | 需求、验收标准、候选方案、推荐项、任务、自测、审查、交付报告 |
| 分析与方案 | `analysis.md` | 复杂/审计模式才拆出：现状分析、修改方案、关键决策(ADR) |
| 循环追踪 | `iteration-log.md` | 失败回流时才拆出：每轮问题/根因/修复/结果 |
| 执行 | `tasks.md` | 完整审计模式可选：任务清单与状态 |
| 交付 | `final-report.md` | 完整审计模式可选：最终交付报告 |
| 索引 | `README.md` | 完整审计模式可选：目录导航、文件作用说明、当前进度 |

默认用 `references/loop-template.md`。拆分文件时使用 `references/analysis-template.md`、`references/iteration-log-template.md`、`references/tasks-template.md`、`references/final-report-template.md`、`references/readme-template.md`。进入任一阶段前先读取 `loop.md` 与已拆出的相关产物，确保基于最新事实而非记忆推进。

## 边界

能做什么、必须先确认什么、绝不做什么——完整规则见 **`references/boundaries.md`**，进入编码（步骤 6）前务必通读。要点：只做需求范围内的改动；破坏性操作（删文件、改 schema、改依赖、改 CI/部署、改公共 API 契约）和提交/推送代码必须先经用户确认；不靠掩盖手段伪装通过。

## 交付汇报

收敛后在 `loop.md` 写完整交付报告，并向用户汇报：完成了什么 / 改了哪些文件 / 跑了哪些验证命令及结果 / 经过几轮循环修了哪些问题 / 剩余风险 / 是否达到可交付状态。只有完整审计模式或用户明确要求时，才另存 `final-report.md`。

## 步骤 9 — 复盘与自我进化（条件触发）

本步骤**默认不执行**，只有本次运行"明显不顺"时才触发，用于让 skill 从真实使用中纠偏、完善、优化自己。完整机制见 `references/evolution.md`。

**触发信号**（命中任一即进入；否则跳过、正常结束）：
- 自适应循环回流 ≥ 2 轮
- 任一确认门被用户否决或要求大改
- grilling 中需求理解被反复纠正（≥ 2 次）
- 触达迭代上限而升级
- 过程中用户明确表达对流程 / skill 的不满

**三条硬约束**（务必遵守，详见 evolution.md）：
- **作用域**：只改本 skill 自身目录的文件，绝不碰其他 skill 或用户项目代码。
- **保护区**：8 阶段闭环、边界安全规则、进化机制本身、description 触发语义**禁止自我修改**；只允许进化措辞、grilling 问法、模板与 checklist 等非结构、非安全部分。
- **独立评估**：用独立 critic（优先子 agent，无则切换对抗视角自审）判断"偶发 vs 系统性"，只改系统性问题，避免过拟合单次运行。

**流程**：收集摩擦点 → critic 评估 → 产出改进提议（diff + 理由 + 风险，过滤掉触碰保护区的）→ **【审批门】展示给用户逐条批准** → 备份到 `_evolution/backups/<时间戳>/` → 应用 → 跑 `python scripts/structure_check.py` 自检（Unix 环境也可跑 `scripts/structure-check.sh` 包装脚本）→ 通过则记 `_evolution/CHANGELOG.md`，失败则自动回滚并报告。

## 参考文档

方法类：
- **`references/grilling.md`** — 步骤 2、4 两个门的盘问式访谈方法（grilling）
- **`references/boundaries.md`** — 能做/需确认/不能做的完整边界规则（编码前必读）
- **`references/review-checklist.md`** — 自测与代码审查的逐项清单
- **`references/evolution.md`** — 步骤 9 自我进化机制（触发 / 保护区 / 审批 / 自检 / 回滚）

脚本类：
- **`scripts/structure_check.py`** — 跨平台结构 + 保护区不变量回归测试（自我进化后自动运行，失败即回滚）
- **`scripts/structure-check.sh`** — Unix 包装脚本，调用 `structure_check.py`

产物模板类（生成到 `dev-loop/YYYY-MM-DD/<需求短名>/`）：
- **`references/loop-template.md`** — 默认主产物 loop.md 模板
- **`references/analysis-template.md`** — 复杂/审计模式拆出的 analysis.md 模板
- **`references/iteration-log-template.md`** — 失败回流时拆出的 iteration-log.md 模板
- **`references/tasks-template.md`** — 完整审计模式可选 tasks.md 模板
- **`references/final-report-template.md`** — 完整审计模式可选 final-report.md 模板
- **`references/readme-template.md`** — 完整审计模式可选 README.md 模板
