---
name: adaptive-dev-loop
description: 当用户通过 /adaptive-dev-loop 显式唤醒、或丢入一个需要"从需求到可交付代码"完整落地的开发需求时使用本 skill。它驱动 AI 走完一条自适应研发闭环：需求接受 → 需求澄清 → 理解项目与影响分析 → 修改方案设计 → 任务拆分 → 编码实现 → 自测 → 代码审查；当自测或审查发现问题时，按问题根因自动回流到对应阶段循环修复，直到收敛或触达迭代上限。本 skill 会真实修改代码，属于重型、长耗时操作。务必使用本 skill 处理任何"实现这个需求 / 把这个功能做完 / 改完并自测自审"类的请求。
---

# 自适应研发闭环 (Adaptive Dev Loop)

把一个开发需求从"接收"一路推进到"自测、自审通过、可交付"的闭环流程。核心特征是**自适应循环**：自测或代码审查发现问题时，按问题根因自动回流到对应阶段重新修复，直到收敛或触达迭代上限。

本 skill 由用户**显式唤醒**（如 `/adaptive-dev-loop 给订单模块加批量导出`）。一旦唤醒即视为用户已授权走完整流程，无需再做规模分流判断——直接进入下面的步骤。本 skill **会真实修改项目代码**，属于重型、长耗时操作。

## 工作原则（先建立心智，再执行步骤）

这些原则贯穿全程，比单个步骤更重要：

- **每个阶段都有可检查的产物，并落成文件。** 长流程容易丢上下文，把需求、分析、任务、迭代日志写进项目根下的产物目录 `dev-loop/<需求短名>/`（见"产物目录"），让每一轮都能基于事实而非记忆推进。
- **两个关键门用 grilling 盘问后再确认。** "需求澄清"和"修改方案设计"这两个门，先用盘问式访谈（`references/grilling.md`）和用户讨论清楚、达成共识，再输出"内容 + 验收标准"做二次确认；其余阶段（编码、自测、审查、循环修复）自动推进，不打断执行节奏。
- **修不好就升级给人，绝不无限重试。** 到达迭代上限或同一问题反复出现时，停下来向用户汇报根因和卡点，而不是继续盲目尝试。
- **遵循现有约定，不擅自扩大范围。** 模仿周边代码的风格与结构；只做需求范围内的事，发现"顺手能改的别的东西"先记录、不动手。
- **不靠掩盖来"修复"。** 不通过改测试断言、跳过用例、吞掉异常来让流程显得通过。详见 `references/boundaries.md`。

## 阶段与产物总览

| # | 阶段 | 关键产物 | 门 |
|---|------|----------|-----|
| 1 | 需求接受 | 在 `dev-loop/` 下建需求子目录，初始化 `README.md` 与 `requirement.md` | — |
| 2 | 需求澄清 | grilling 盘问 + 确认后的需求理解与验收标准 | **grilling+确认门** |
| 3 | 理解项目与影响分析 | 写入 `analysis.md`：需求理解 / 当前项目现状（含原有规范）/ 涉及范围 | — |
| 4 | 修改方案设计 | 续写 `analysis.md`：修改方案 / 具体修改内容 / 风险点 / 验证方式 / 最终结论 | **grilling+确认门** |
| 5 | 任务拆分 | `tasks.md`（可独立验证的小任务 + 验收标准） | — |
| 6 | 编码实现 | 代码改动 + 勾掉 tasks.md 中的任务 | — |
| 7 | 自测 | 构建/测试/lint 结果，记入 `iteration-log.md` | — |
| 8 | 代码审查 | 审查结论（按 `references/review-checklist.md`） | — |
| ↺ | 自适应循环 | 发现问题 → 按根因回流到 4/5/6 或 2 → 重新走到 7、8 | — |
| ✓ | 交付汇报 | `final-report.md` | — |

## 详细步骤

### 步骤 1 — 需求接受
取得需求。若用户调用时已带需求文本，以该文本为准；若未提供，先追问"请描述你要实现的需求（目标 + 范围）"。

为本次需求起一个简短的 kebab-case 名称，在项目根目录下创建产物目录 `dev-loop/<需求短名>/`（若 `dev-loop/` 不存在则一并创建）；用 `references/readme-template.md` 初始化该目录的 `README.md`，并把原始需求写入其中的 `requirement.md`。本次运行的所有产物都放在这个子目录里。

### 步骤 2 — 需求澄清 〔grilling + 确认门〕
用 **grilling 盘问式访谈**澄清需求（方法见 `references/grilling.md`）：一次只问一个聚焦问题、每个问题附上自己的推荐答案、能在代码里查到的先查不问，挑战有风险的假设，逐项理清目标、范围、边界情况、验收标准，直到达成共识。盘问过程中把结论与关键决策实时补进 `requirement.md`。

**确认门**：盘问收敛后，把"最终需求理解 + 验收标准"完整输出给用户做二次确认。用户确认后才进入步骤 3。理解不一致会让后续整条路跑偏，这道门不能省。

### 步骤 3 — 理解项目与影响分析
**编码前先理解项目，再分析影响——顺序不能颠倒。** 这是非破坏性的事实收集，全部写入一份分析文档 `analysis.md`（完整结构见 `references/analysis-template.md`），按以下章节推进：

1. **需求理解**：把确认后的需求目标、范围、验收标准落到文档。
2. **当前项目现状**：实地阅读相关代码后再写——技术栈、目录与分层、**项目原有的规范与约定**（命名风格、代码组织、错误处理、日志、配置、测试组织与运行方式等），以及与本需求相关的现有实现是如何工作的。**这一节必须针对当前项目实际分析，不能写通用套话。**
3. **涉及范围**：会改到的文件/模块、受影响的调用方与上下游、相关测试、潜在回归点。

核心要求：本次改动要**融入并遵循项目原有规范，而不是另起一套新规范**。后续的方案与具体修改都以本节提炼出的现状为准绳。

### 步骤 4 — 修改方案设计 〔grilling + 确认门〕
在同一份 `analysis.md` 中续写方案部分，**全程沿用步骤 3 提炼的原有规范**：

4. **修改方案**：在现有结构内如何实现的核心思路、关键改动点、接口/数据结构变化、取舍与放弃的备选方案。
5. **具体修改内容**：细化到文件/函数级——每个文件改什么、新增什么，并说明如何遵循原有规范。
6. **风险点**：技术风险、回归风险、对其他模块的影响、需用户拍板的高风险项。
7. **验证方式**：如何自测（构建/测试/lint）、每条验收标准如何验证、需新增或修改哪些测试。
8. **最终结论**：方案可行性与复杂度评估，以及需要用户在确认门拍板的事项。

**grilling + 两段式确认门**（方法见 `references/grilling.md`）：
1. **讨论选型**：就关键决策点逐个 grilling 盘问——给出 2–3 个候选方案及各自利弊和推荐项，一次聚焦一个分歧点，挑战风险假设，能查代码先查。把讨论与决策理由（ADR 式）记入 `analysis.md`，让用户选定方案。
2. **正式确认**：方案选定后，把【完整修改方案 + 验收标准】输出给用户二次确认，尤其当涉及架构、公共接口、数据库 schema、依赖变更时。用户确认无误后，才进入下一轮（任务拆分与编码）。

### 步骤 5 — 任务拆分
把方案拆成**可独立验证**的小任务，写入 `tasks.md`，每个任务带一句话验收标准。任务粒度以"改完能单独自测"为宜。

### 步骤 6 — 编码实现
按 tasks.md 逐个实现，遵循现有代码风格与约定，小步推进。每完成一个任务就在 tasks.md 勾掉并简注改了什么。

### 步骤 7 — 自测
按 `references/review-checklist.md` 的"自测"部分执行：跑构建、测试、lint，验证每个任务的验收标准与需求的整体验收标准。把命令与结果（通过/失败、失败详情）记入 `iteration-log.md`。**"通过"必须有客观依据**（命令退出码、测试输出），不能凭感觉判定。

### 步骤 8 — 代码审查
以独立审查视角，按 `references/review-checklist.md` 的"审查"部分逐项检查：正确性、是否符合现有约定、是否引入回归、边界与错误处理、是否偏离需求范围。把发现的问题记入 `iteration-log.md`。

## 自适应循环机制（核心）

自测（步骤 7）或审查（步骤 8）发现问题后，**按根因分级回流**，而不是一律推倒重来：

| 问题根因 | 回流到 |
|----------|--------|
| 编码 bug、测试失败、实现写错 | 步骤 6 编码实现 |
| 漏改了某处、任务覆盖不全、有遗漏 | 步骤 5 任务拆分 |
| 方案本身有设计缺陷（架构/接口/数据流错误） | 步骤 4 修改方案设计 |
| 发现需求理解有误 | 步骤 2 需求澄清（重新触发确认门，需问用户） |

回流时**带上"这一轮为什么失败"的信息**重新进入该阶段，避免重复犯同样的错。每轮都在 `iteration-log.md` 记录：发现的问题 → 根因定位 → 回到哪一步 → 做了什么修复 → 结果。修复后重新走到步骤 7、8。

**终止与升级**（防死循环，必须遵守）：
- **收敛**：自测与审查均通过 → 进入交付汇报。
- **迭代上限**：默认最多 3 轮循环仍未收敛 → 停止，升级给用户。
- **同一问题反复**：同一问题连续出现 2 次 → 立即升级（说明当前路径无效，需要人介入）。
- **升级时**：把当前状态、已尝试的修复、根因判断、建议方向汇报给用户，由用户决策，而非继续盲目重试。

## 产物目录

本 skill 的所有产物（md）集中存放在**项目根目录下新建的 `dev-loop/` 文件夹**。每次运行按需求建一个子目录（步骤 1 起的 kebab-case 短名），互不覆盖、便于归档多个需求：

```
dev-loop/                         本 skill 所有产物的归档目录（项目根下）
└── <需求短名>/                    每个需求一个子目录
    ├── README.md                 本批产物索引：文件清单 + 各文件作用 + 当前进度
    ├── requirement.md            需求原文 + grilling 澄清结论 + 验收标准
    ├── analysis.md               项目现状(含原有规范) + 修改方案 + ADR（步骤 3、4）
    ├── tasks.md                  任务拆分与进度（步骤 5、6）
    ├── iteration-log.md          自适应循环每轮记录（步骤 7、8 及回流）
    └── final-report.md           交付报告（收敛后）
```

各文件作用（分类）：

| 类别 | 文件 | 作用 |
|------|------|------|
| 索引 | `README.md` | 目录导航、文件作用说明、当前进度 |
| 需求 | `requirement.md` | 需求与验收标准 |
| 分析与方案 | `analysis.md` | 现状分析、修改方案、关键决策(ADR) |
| 执行 | `tasks.md` | 任务清单与状态 |
| 循环追踪 | `iteration-log.md` | 每轮问题/根因/修复/结果 |
| 交付 | `final-report.md` | 最终交付报告 |

各文件的结构模板见 `references/`。进入任一阶段前先读取相关产物，确保基于最新事实而非记忆推进。

## 边界

能做什么、必须先确认什么、绝不做什么——完整规则见 **`references/boundaries.md`**，进入编码（步骤 6）前务必通读。要点：只做需求范围内的改动；破坏性操作（删文件、改 schema、改依赖、改 CI/部署、改公共 API 契约）和提交/推送代码必须先经用户确认；不靠掩盖手段伪装通过。

## 交付汇报

收敛后产出 `final-report.md` 并向用户汇报：完成了什么 / 改了哪些文件 / 跑了哪些验证命令及结果 / 经过几轮循环修了哪些问题 / 剩余风险 / 是否达到可交付状态。

## 参考文档

方法类：
- **`references/grilling.md`** — 步骤 2、4 两个门的盘问式访谈方法（grilling）
- **`references/boundaries.md`** — 能做/需确认/不能做的完整边界规则（编码前必读）
- **`references/review-checklist.md`** — 自测与代码审查的逐项清单

产物模板类（生成到 `dev-loop/<需求短名>/`）：
- **`references/readme-template.md`** — 产物目录索引 README.md 模板
- **`references/analysis-template.md`** — 分析与方案 analysis.md（8 章 + ADR）模板
- **`references/tasks-template.md`** — 任务拆分 tasks.md 模板
- **`references/iteration-log-template.md`** — 迭代日志 iteration-log.md 模板
- **`references/final-report-template.md`** — 交付报告 final-report.md 模板
